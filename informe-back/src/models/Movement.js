export class Movement {
    constructor({ id, fecha, tipo, monto, descripcion }) {
        this.id = id;
        this.fecha = fecha;
        this.tipo = tipo;
        this.monto = monto;
        this.descripcion = descripcion;
    }
}